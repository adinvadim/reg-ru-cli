# Domain Docs

This is a single-context repository. Before exploring or changing the codebase, read `CONTEXT.md` at the repository root.

Use the terminology and constraints defined there consistently. If the file contains no relevant guidance yet, proceed without requiring it to be expanded first.
