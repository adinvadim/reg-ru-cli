# Changelog

## Unreleased

- Scaffold the `regru` Go CLI with stable output, error, safety, authentication, timeout, completion, and capability-placeholder contracts.
- Add provider-neutral multi-account profiles, strict user/project TOML boundaries, redacted account and capability commands, and command-scoped credential envelopes over stdin.
